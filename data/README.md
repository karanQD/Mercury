# Data

This directory is intentionally empty by default. Store small synthetic PCAPs, replay fixtures, and benchmark payloads here. For large captures, keep data outside the repository and point `mercury_main --pcap` to the file path.
